-- Testing the Core suitetell application "MCL 4.2"	if (exists window "New") then		set index of window "new" to 1	else		make new document	end if	paste "Hello Script!"	duplicate characters 1 thru 6 of document "new"	tell document "New"		if index is not 0 then set index to 0 -- brings window to front		delete (characters 12 thru 18)		set first character to Â			characters 1 thru 5		set last character to Â			(count the characters) as string		set middle character to "<>"		if modified then close saving in file "temp"	end tell	if exists file "temp" then		open file "temp"	end ifend tell